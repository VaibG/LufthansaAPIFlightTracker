# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'LufthansaFlightTracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LufthansaFlightTracker
  pod 'Alamofire', '~> 4.0'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'ObjectMapper'
  pod 'SkyFloatingLabelTextField', '~> 3.0'


  target 'LufthansaFlightTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LufthansaFlightTrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
